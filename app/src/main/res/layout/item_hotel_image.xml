<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="155dp"
    android:layout_height="150dp">

    <ImageView
        android:id="@+id/hotel_image_view"
        android:layout_width="150dp"
        android:layout_height="150dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:elevation="-1dp"
        android:indeterminateTint="#F66C2C"
        app:layout_constraintBottom_toBottomOf="@+id/hotel_image_view"
        app:layout_constraintEnd_toEndOf="@+id/hotel_image_view"
        app:layout_constraintStart_toStartOf="@+id/hotel_image_view"
        app:layout_constraintTop_toTopOf="@+id/hotel_image_view" />

</androidx.constraintlayout.widget.ConstraintLayout>
